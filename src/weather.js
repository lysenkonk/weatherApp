 class Weather{
     constructor(temp, icon, desc, wind, humidity){
         this.temp = temp;
         this.icon = icon;
         this.desc = desc;
         this.wind = wind;
         this.humidity = humidity;
     }
     
 }

 class Forecast{
     constructor(appIdWeatherApi, url, )
 }